From Risk Factors, Liquidity, Notes, and transcript Q&A, extract:
- liquidity position and near-term maturities
- covenants (thresholds vs current)
- distress signals (going-concern, auditor emphasis, waivers/restructuring)

Each line requires at least one citation token in format:
`:{doc}:{page_or_section}|{hh:mm:ss_if_call}`.
